===== FINAL METRICS =====
N: 50
accuracy: 0.62
never_diagnosed: 4
OER: 0.28
ECE: 0.246
avg_confidence: 0.846
UAR: 0.12
EPH_intended: 2.16
EPH_realized: 1.88
EPH_blocked: 0.28
EPH_reduction_%: 13.0
high_harm_missed: 0.16
avg_tests_requested_by_llm: 5.54
avg_tests_found_in_data: 1.54
avg_diagnostic_precision: 0.41
cases_with_tests: 33
rx_attempt_rate: 0.46
rx_failure_rate: 0.0
avg_turns: 7.5
avg_decision_delay: 7.2
premature_action_rate: 0.1
test_action_mismatch_rate: 0.18



import pandas as pd
from pathlib import Path

def audit_experiment_folders(experiment_dirs):
    """
    Counts the number of summary files in each language folder for every experiment.
    """
    results = []
    
    for exp_dir in experiment_dirs:
        exp_path = Path(exp_dir)
        if not exp_path.exists():
            print(f"Warning: Directory not found -> {exp_dir}")
            continue
            
        # Get the experiment name (e.g., 'zeroshot', 'fewshot')
        exp_name = exp_path.name 
        
        for lang_folder in exp_path.iterdir():
            if not lang_folder.is_dir() or lang_folder.name.startswith('.'):
                continue
                
            language = lang_folder.name
            summary_dir = lang_folder / "Summary_Text"
            
            count = 0
            if summary_dir.exists():
                count = len(list(summary_dir.glob("*_summary.txt")))
                
            results.append({
                "Language": language,
                "Experiment": exp_name,
                "File_Count": count
            })
            
    # Convert to a DataFrame and create a clean pivot table
    df = pd.DataFrame(results)
    if not df.empty:
        # Rows = Languages, Columns = Experiments
        matrix = df.pivot(index="Language", columns="Experiment", values="File_Count").fillna(0).astype(int)
        print("\n=== File Count Matrix ===")
        print(matrix.to_string())
    else:
        print("No summary files found.")

# ==========================================
# Execution Setup
# ==========================================
EXPERIMENT_FOLDERS = [
    "EACL/Qwen_exp/zeroshot",
    "EACL/Qwen_exp/fewshot",
    "EACL/Qwen_exp/translated"
    # Add your Mistral paths here
]

audit_experiment_folders(EXPERIMENT_FOLDERS)
