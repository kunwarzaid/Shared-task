import os
import json
import torch
from datasets import Dataset
from transformers import (
    AutoTokenizer,
    AutoModelForCausalLM,
    TrainingArguments,
    Trainer,
    BitsAndBytesConfig
)
from peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training

# ==========================================================
# CONFIG
# ==========================================================

BASE_MODEL = "Qwen/Qwen2.5-7B-Instruct"

DATA_DIR = "/workspace/data/KZ_2117574/SharedTask_NLPAI4Health_Train&dev_set"
OUTPUT_DIR = "./qwen_medical_summary_ft"

CTX_MAX = 16000
LR = 2e-5
EPOCHS = 3

BATCH_SIZE = 1
GRAD_ACCUM = 16

SEED = 42
torch.manual_seed(SEED)


# ==========================================================
# TOKENIZER
# ==========================================================

tokenizer = AutoTokenizer.from_pretrained(
    BASE_MODEL,
    trust_remote_code=True
)

if tokenizer.pad_token is None:
    tokenizer.pad_token = tokenizer.eos_token

tokenizer.padding_side = "right"


# ==========================================================
# DATA LOADING
# ==========================================================

def read_jsonl(path):

    rows = []

    with open(path, "r", encoding="utf8") as f:
        for line in f:
            rows.append(json.loads(line))

    return rows


def join_dialogue(rows):

    parts = []

    for r in rows:

        val = r.get("dialogue", "")

        if isinstance(val, list):
            val = " ".join(val)

        parts.append(val)

    return "\n".join(parts)


def build_examples(split_dir):

    examples = []

    for lang in os.listdir(split_dir):

        lang_dir = os.path.join(split_dir, lang)

        dlg_dir = os.path.join(lang_dir, "Dialogues")
        sum_dir = os.path.join(lang_dir, "Summary_Text")

        if not os.path.isdir(dlg_dir):
            continue

        for fn in os.listdir(dlg_dir):

            if not fn.endswith(".jsonl"):
                continue

            rows = read_jsonl(os.path.join(dlg_dir, fn))
            dialogue = join_dialogue(rows)

            sum_path = os.path.join(
                sum_dir,
                fn.replace(".jsonl", "_summary.txt")
            )

            if not os.path.exists(sum_path):
                continue

            summary = open(sum_path).read().strip()

            examples.append(
                {
                    "dialogue": dialogue,
                    "summary": summary
                }
            )

    return examples


train_raw = build_examples(os.path.join(DATA_DIR, "train_split"))
dev_raw = build_examples(os.path.join(DATA_DIR, "dev"))

print("Train:", len(train_raw))
print("Dev:", len(dev_raw))

train_dataset = Dataset.from_list(train_raw)
dev_dataset = Dataset.from_list(dev_raw)


# ==========================================================
# PROMPT
# ==========================================================

def build_prompt(dialogue):

    return f"""
Below is a doctor–patient conversation.

The dialogue may be written in Hindi, Marathi, Bangla, or English.

Write a concise and faithful clinical summary in English.

Rules:
- Only include information explicitly mentioned.
- Do not infer diagnoses or medications.
- Preserve negations such as "denies fever".

Dialogue:
{dialogue}
"""


# ==========================================================
# TOKENIZATION
# ==========================================================

def tokenize(example):

    prompt_text = build_prompt(example["dialogue"])

    messages = [
        {"role": "system", "content": "You are a clinical documentation assistant."},
        {"role": "user", "content": prompt_text}
    ]

    prompt = tokenizer.apply_chat_template(
        messages,
        tokenize=False,
        add_generation_prompt=True
    )

    summary = example["summary"] + tokenizer.eos_token

    prompt_ids = tokenizer.encode(prompt, add_special_tokens=False)
    summary_ids = tokenizer.encode(summary, add_special_tokens=False)

    if len(prompt_ids) + len(summary_ids) > CTX_MAX:
        return None

    input_ids = prompt_ids + summary_ids

    labels = [-100] * len(prompt_ids) + summary_ids
    labels = labels[:len(input_ids)]

    return {
        "input_ids": input_ids,
        "attention_mask": [1] * len(input_ids),
        "labels": labels
    }


train_dataset = train_dataset.map(tokenize)
dev_dataset = dev_dataset.map(tokenize)

train_dataset = train_dataset.filter(lambda x: x is not None)
dev_dataset = dev_dataset.filter(lambda x: x is not None)


# ==========================================================
# COLLATOR
# ==========================================================

def collate(batch):

    max_len = max(len(x["input_ids"]) for x in batch)

    input_ids = []
    attention_mask = []
    labels = []

    for x in batch:

        pad = max_len - len(x["input_ids"])

        input_ids.append(
            x["input_ids"] + [tokenizer.pad_token_id] * pad
        )

        attention_mask.append(
            x["attention_mask"] + [0] * pad
        )

        labels.append(
            x["labels"] + [-100] * pad
        )

    return {
        "input_ids": torch.tensor(input_ids),
        "attention_mask": torch.tensor(attention_mask),
        "labels": torch.tensor(labels)
    }


# ==========================================================
# MODEL
# ==========================================================

bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.float16
)

model = AutoModelForCausalLM.from_pretrained(
    BASE_MODEL,
    device_map="auto",
    quantization_config=bnb_config,
    trust_remote_code=True
)

model = prepare_model_for_kbit_training(model)
model.gradient_checkpointing_enable()


lora_config = LoraConfig(

    r=8,
    lora_alpha=32,
    lora_dropout=0.05,

    target_modules=[
        "q_proj",
        "k_proj",
        "v_proj",
        "o_proj",
        "gate_proj",
        "up_proj",
        "down_proj"
    ],

    task_type="CAUSAL_LM"
)

model = get_peft_model(model, lora_config)
model.print_trainable_parameters()


# ==========================================================
# TRAINING
# ==========================================================

training_args = TrainingArguments(

    output_dir=OUTPUT_DIR,

    per_device_train_batch_size=BATCH_SIZE,
    gradient_accumulation_steps=GRAD_ACCUM,

    num_train_epochs=EPOCHS,

    learning_rate=LR,

    fp16=True,

    logging_steps=50,

    evaluation_strategy="steps",
    eval_steps=500,

    save_steps=500,
    save_total_limit=2,

    report_to="none"
)

trainer = Trainer(

    model=model,

    args=training_args,

    train_dataset=train_dataset,
    eval_dataset=dev_dataset,

    data_collator=collate
)

trainer.train()

trainer.save_model(OUTPUT_DIR)
tokenizer.save_pretrained(OUTPUT_DIR)
