
# ============================================================
# CONFIG
# ============================================================

MODEL_NAME = "mistralai/Mistral-7B-Instruct-v0.2"
# MODEL_NAME = "Qwen/Qwen2.5-7B-Instruct"

MODEL_TYPE = "mistral"  # "qwen" or "mistral"

TEST_DIR = "/workspace/data/KZ_2117574/SharedTask_NLPAI4Health_Train&dev_set/translated_data"
OUTPUT_DIR = f"/workspace/data/KZ_2117574/EACL/Mistral_exp/translated/{MODEL_TYPE}"

TARGET_LANGS = ["Bangla", "English", "Hindi", "Marathi","Telugu","Tamil","Kannada","Gujrati","Dogri","Assamese"]

CTX_MAX = 16000
MAX_NEW_TOKENS_SUMMARY = 1024

# ============================================================
# PROMPT
# ============================================================

ZERO_SHOT_PROMPT_TEMPLATE = """
You are a clinical documentation assistant.

{language_line}

Important: The dialogue is written in English (translated from original language).

Task: Write a clinical summary of the dialogue.

Rules:
- Use ONLY information explicitly stated in the dialogue.
- Do NOT infer, assume, or add missing details.
- Do NOT introduce diagnoses, medications, or durations unless explicitly mentioned.
- Preserve negations exactly.
- If information is not present, do not include it.
- If unsure about any detail, omit it.
- Do NOT translate or reinterpret the dialogue.

Write a clear, complete, and concise clinical summary.

Output:
[Summary]
<summary>

Dialogue:
{dialogue}
"""

# ============================================================
# UTILS
# ============================================================

def tprint(msg):
    print(f"[{time.strftime('%H:%M:%S')}] {msg}", flush=True)

def safe_read_jsonl(path):
    rows = []
    with open(path, "r", encoding="utf-8", errors="replace") as f:
        for line in f:
            try:
                rows.append(json.loads(line.strip()))
            except:
                continue
    return rows

def write_text(path, text):
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text.strip() + "\n", encoding="utf-8")

def normalize_transliterated(text):
    return text

def count_tokens(tokenizer, text):
    return len(tokenizer.encode(text, add_special_tokens=False))

# ============================================================
# MODEL
# ============================================================

def load_model():
    tokenizer = AutoTokenizer.from_pretrained(MODEL_NAME, use_fast=True)

    if tokenizer.pad_token is None:
        tokenizer.pad_token = tokenizer.eos_token

    quant_cfg = BitsAndBytesConfig(
        load_in_4bit=True,
        bnb_4bit_quant_type="nf4",
        bnb_4bit_compute_dtype=torch.float16
    )

    model = AutoModelForCausalLM.from_pretrained(
        MODEL_NAME,
        device_map="auto",
        quantization_config=quant_cfg,
        torch_dtype=torch.float16
    )

    model.eval()
    return model, tokenizer

# ============================================================
# PROMPT BUILDER
# ============================================================

def build_prompt(user_prompt):
    if MODEL_TYPE == "qwen":
        return [{"role": "user", "content": user_prompt}]
    
    elif MODEL_TYPE == "mistral":
        # Mistral instruction format
        return f"<s>[INST] {user_prompt.strip()} [/INST]"

# ============================================================
# GENERATION
# ============================================================

def generate(model, tokenizer, prompt):

    if MODEL_TYPE == "qwen":
        prompt_text = tokenizer.apply_chat_template(
            prompt,
            tokenize=False,
            add_generation_prompt=True
        )
    else:
        prompt_text = prompt

    inputs = tokenizer(
        prompt_text,
        return_tensors="pt",
        truncation=False
    ).to(model.device)

    with torch.no_grad():
        output = model.generate(
            **inputs,
            max_new_tokens=MAX_NEW_TOKENS_SUMMARY,
            do_sample=False,
            temperature=0.0,
            num_beams=1,
            pad_token_id=tokenizer.eos_token_id,
            eos_token_id=tokenizer.eos_token_id
        )

    gen_ids = output[0][inputs.input_ids.shape[-1]:]
    return tokenizer.decode(gen_ids, skip_special_tokens=True).strip(), prompt_text

# ============================================================
# MAIN PIPELINE
# ============================================================

def run_summary_only():
    model, tokenizer = load_model()

    skipped = 0
    processed = 0

    langs = [
        p for p in Path(TEST_DIR).iterdir()
        if p.is_dir() and p.name in TARGET_LANGS
    ]

    for lang_dir in langs:
        lang = lang_dir.name
        tprint(f"Processing language: {lang}")

        dlg_dir = lang_dir / "Dialogues"
        out_dir = Path(OUTPUT_DIR) / lang / "Summary_Text"

        files = sorted(dlg_dir.glob("*.jsonl"))

        for f in tqdm(files, desc=lang):
            out_file = out_dir / f"{f.stem}_summary.txt"
            if out_file.exists():
                continue

            rows = safe_read_jsonl(f)

            dialogue = " ".join(
                r.get("dialogue", "") for r in rows if isinstance(r, dict)
            )

            if lang != "English":
                dialogue = normalize_transliterated(dialogue)

            if lang == "English":
                language_line = "The dialogue is in English."
            else:
                language_line = f"The dialogue is written/translated in English and represents spoken {lang} (Romanized {lang})."

            user_prompt = ZERO_SHOT_PROMPT_TEMPLATE.format(
                dialogue=dialogue,
                language_line=language_line
            )

            prompt = build_prompt(user_prompt)

            # ============================
            # CTX CHECK
            # ============================

            if MODEL_TYPE == "qwen":
                prompt_text = tokenizer.apply_chat_template(
                    prompt,
                    tokenize=False,
                    add_generation_prompt=True
                )
            else:
                prompt_text = prompt

            input_tokens = count_tokens(tokenizer, prompt_text)

            if input_tokens + MAX_NEW_TOKENS_SUMMARY > CTX_MAX:
                skipped += 1
                print(f"[SKIPPED] {f.name} | tokens={input_tokens}")
                continue

            try:
                summary, _ = generate(model, tokenizer, prompt)
                write_text(out_file, summary)
                processed += 1

            except RuntimeError as e:
                print(f"[ERROR] {f.name}: {e}")
                torch.cuda.empty_cache()
                continue

        torch.cuda.empty_cache()

    tprint(f"Done. Processed={processed}, Skipped={skipped}")

# ============================================================
# ENTRY
# ============================================================

if __name__ == "__main__":
    run_summary_only()
