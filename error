import json
from pathlib import Path
from tqdm import tqdm
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM
import torch

# ==========================
# CONFIG
# ==========================

INPUT_DIR = "selected_data"
OUTPUT_DIR = "translated_data"

MODEL_NAME = "ai4bharat/indictrans2-indic-en"

LANG_TAGS = {
    "Hindi": "hin_Deva",
    "Marathi": "mar_Deva",
    "Bangla": "ben_Beng",
    "English": "eng_Latn"
}

DEVICE = "cuda" if torch.cuda.is_available() else "cpu"

# ==========================
# LOAD MODEL
# ==========================

print("Loading IndicTrans2 model...")

tokenizer = AutoTokenizer.from_pretrained(MODEL_NAME, use_fast=False)
model = AutoModelForSeq2SeqLM.from_pretrained(MODEL_NAME).to(DEVICE)

# ==========================
# TRANSLATION FUNCTION
# ==========================

def translate_text(text, src_lang):

    if src_lang == "eng_Latn":
        return text

    formatted = f"{src_lang} eng_Latn {text}"

    inputs = tokenizer(
        formatted,
        return_tensors="pt",
        truncation=True,
        max_length=4096
    ).to(DEVICE)

    outputs = model.generate(
        **inputs,
        max_length=4096,
        num_beams=4
    )

    translated = tokenizer.decode(
        outputs[0],
        skip_special_tokens=True
    )

    return translated


# ==========================
# MAIN TRANSLATION LOOP
# ==========================

def main():

    for lang, tag in LANG_TAGS.items():

        input_dir = Path(INPUT_DIR) / lang / "Dialogues"
        output_dir = Path(OUTPUT_DIR) / lang / "Dialogues"

        output_dir.mkdir(parents=True, exist_ok=True)

        files = list(input_dir.glob("*.jsonl"))

        print(f"\nTranslating {lang} ({len(files)} files)")

        for f in tqdm(files):

            translated_lines = []

            with open(f, encoding="utf-8") as file:

                for line in file:

                    try:
                        obj = json.loads(line)

                        speaker = obj.get("speaker", "")
                        text = obj.get("dialogue", "")

                        translated_text = translate_text(text, tag)

                        new_obj = {
                            "speaker": speaker,
                            "dialogue": translated_text
                        }

                        translated_lines.append(json.dumps(new_obj, ensure_ascii=False))

                    except:
                        translated_lines.append(line.strip())

            output_path = output_dir / f.name

            with open(output_path, "w", encoding="utf-8") as out:

                for l in translated_lines:
                    out.write(l + "\n")


if __name__ == "__main__":
    main()
