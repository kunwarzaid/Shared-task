Each JSON field is treated as an independent question–answer task, allowing flexible regeneration of missing fields.  
This modular design improved structured coverage $\sim$3× compared to single-shot JSON generation.
