# Check missing values
print(df.isna().sum())

# Summary statistics for numeric metrics
print(df.describe())

# Check number of unique diagnoses (if loading per-case results)
if "diagnosis" in df.columns:
    print("\nUnique diagnoses found:")
    print(df["diagnosis"].value_counts().head(10))
