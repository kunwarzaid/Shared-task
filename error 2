def count_tokens(text, src_tag):

    tokens = tokenizer(
        text,
        src_lang=src_tag,
        tgt_lang="eng_Latn",
        truncation=False,
        add_special_tokens=False
    )

    return len(tokens["input_ids"])
